{#def request, user=None, projects=[] #}
{#import "../layouts/public.jinja" as PublicLayout #}
{#import "../projects/project-card.jinja" as ProjectCard #}

<PublicLayout request={{ request }} user={{ user }} title="Projects" current_page="projects">
    <div class="space-y-8">
        <header>
            <h1 class="text-3xl font-bold text-white mb-2">Projects</h1>
            <p class="text-gray-400">A collection of projects I've worked on.</p>
        </header>

        <div class="space-y-4">
            {% for project in projects %}
            <ProjectCard project={{ project }} />
            {% else %}
            <div class="text-center py-12">
                <p class="text-gray-500">No projects yet.</p>
            </div>
            {% endfor %}
        </div>
    </div>
</PublicLayout>
